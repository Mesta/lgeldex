<div class="container">
  <%= pageTitle 'Mode', 'Détails' %>

  <p>
    <strong>Nom :</strong>
    <%= @mode.nom %>
  </p>

  <%= link_to 'Modifier', edit_mode_path(@mode), class: 'btn btn-default' %>
  <%= link_to 'Retour', modes_path, class: 'btn btn-default' %>

  <hr/>

  <div class="panel panel-default">
    <div class="panel-heading">
      <ul class="list-inline panel-links" style="text-align:left;">
        <li><h1 style="padding:0px;margin:0px"><small>Joueurs</small></h1></li>
      </ul>
    </div>
    <div class="panel-body">
      <table class="table table-hover table-stripped table-condensed">
        <thead>
        <tr>
          <th>Pseudo</th>
        </tr>
        </thead>

        <tbody>
        <% @mode.joueurs.each do |joueur| %>
            <tr>
              <td><%= link_to joueur.pseudo, joueur %></td></td>
            </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>

</div>
